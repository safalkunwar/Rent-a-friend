{
  "name": "sathi-migration",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "migrate:dry-run": "tsx scripts/firebase-migration/index.ts --dry-run",
    "migrate:production": "tsx scripts/firebase-migration/index.ts",
    "migrate:verify": "tsx scripts/firebase-migration/verify.ts",
    "migrate:rollback": "tsx scripts/firebase-migration/rollback.ts"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
